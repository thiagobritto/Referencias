/**
 * O ECMAScript 2016 introduzido Array.prototype.includes em arrays.
 * Isso nos permite verificar se um elemento está presente em um array
 */

const fruits = ["Banana", "Orange", "Apple", "Mango"]

console.log(
  fruits.includes("Mango") // retorna true
);